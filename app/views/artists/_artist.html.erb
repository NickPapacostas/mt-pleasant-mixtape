<script type='text/ng-template' id='artist.html'>
  <a href="#/homepage"><%= image_tag "logo-white.png", {class: "w-hidden-tiny main-logo", width: "152"} %></a>

  <div ng-if="artists">
    <div ng-if="!activeArtist.background_image_url"> <%= image_tag "band-example.jpg", { width: "100%"} %> </div>
    <img ng-if="activeArtist.background_image_url" ng-src="{{activeArtist.background_image_url}}" width="100%"/>
    <div class="band-photo-area">
      <div class="band-info">
        <h6>BAND</h6>
        <h1 class="band-name">{{activeArtist.name}}</h1>
      </div>
    </div>
    <div class="bandinfo">
      <div class="w-embed w-iframe" ng-if="activeArtist.song_url" >
        <iframe width="100%" height="166" scrolling="no" frameborder="no" ng-src="{{activeArtist.song_iframe_url}} "></iframe>
      </div>

      <h2>Story <span class="smallital">of the</span> Band</h2>
      <p>{{activeArtist.story_of_the_band}}</p>

      <h2>Story <span class="smallital">of the</span> Song</h2>
      <p>{{activeArtist.story_of_the_song}}</p>

      <h2>Favorite DC Thing</h2>
      <p>{{activeArtist.favorite_dc_thing}}</p>
    </div>
    <div ng-if="nextArtist">
      <div class="band-photo-area nextband">
        <a class="w-inline-block" href="#/artists/{{nextArtist.id}}">
          <div class="band-info nextband">
            <h6>Next Up...</h6>
            <h1 class="band-name">{{nextArtist.name}}</h1>
          </div>
        </a>
      </div>
    </div>
  </div
</script>
